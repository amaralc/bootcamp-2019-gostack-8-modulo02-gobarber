const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), // caminho para config
  'models-path': resolve(__dirname, 'src', 'app', 'models'), // caminho para models
  'migrations-path': resolve(__dirname,'src','database','migrations'), //caminho para pasta migrations
  'seeders-path':resolve(__dirname,'src','database','seeds'), // caminho para pasta de seeds
};
